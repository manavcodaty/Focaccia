{
  "name": "@face-pass/shared",
  "private": true,
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "react-native": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:build": "rm -rf dist-test && tsc -p tsconfig.test.json",
    "test": "pnpm run test:build && jest --config jest.config.cjs --runInBand"
  },
  "dependencies": {
    "libsodium-wrappers": "0.8.2"
  },
  "peerDependencies": {
    "@more-tech/react-native-libsodium": "1.5.6"
  },
  "peerDependenciesMeta": {
    "@more-tech/react-native-libsodium": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/jest": "30.0.0",
    "@types/node": "25.5.2",
    "jest": "30.3.0",
    "typescript": "6.0.2"
  }
}
